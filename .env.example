# Salin file ini ke .env (atau .env.production untuk Docker) lalu sesuaikan

# Wajib — nilai header x-api-key yang diterima endpoint
API_KEY=your-secret-api-key

# Opsional — port HTTP server (default 3002)
PORT=3002
